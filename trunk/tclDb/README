tclDb is a bit of a grab-bag at the moment.

;Db.tcl: is a snit Metakit wrapper, because I can't love the one distributed with Mk.
;db_tie.tcl: ties arrays to Db, allowing the tcllib tie package to use a Metakit backend
;mkdict_tie.tcl: ties arrays directly to Metakit, allowing the tcllib tie package to use a Metakit backend
;MkDomain.tcl: Wub Domain to query Metakits via Db interface
;mkrecord.tcl: tie an array of dicts to an Mk database without using tcllib tie.
;Mkvfs.tcl: a Mk Virtual File System over snitvfs
;nulluser.tcl: a trivial user system, used for Mkvfs
;tieuser.tcl: a trivial user system using tie - not implemented
;snitvfs.tcl: a snit wrapper to implement a Virtual File System

Colin McCormack. usual tcl license.
